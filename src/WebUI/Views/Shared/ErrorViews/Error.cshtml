@model IEnumerable<string>

@{
    ViewBag.Title = $"{Context.Response.StatusCode} - Error while processing the request";
}

<div class="container text-center justify-content-center mt-4">
    <h2>@Context.Response.StatusCode</h2>
    <h3>Error while processing the request</h3>
    <div>
        @foreach (var error in Model)
        {
            <p class="text-warning text-center fs-5">@error</p>
        }
    </div>
</div>


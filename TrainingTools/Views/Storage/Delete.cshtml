@*@model StorageViewModel 
@{
    ViewBag.Title = "Delete Storage";
    ViewBag.ActionPage = "storages";
}

<div class="container-fluid">
    <form method="post">
        <label class="mb-2 text-warning">Are you sure that you want to delete storage @Model.Name (@Model.Id)?</label>
        <div class="form-group mb-3">
            <label for="register-password">Password</label>
            <input type="password" class="form-control" name="Password" id="register-password" placeholder="" required>
            <small id="password-helper" class="form-text text-muted">Password must have length from 8 to 16 characters</small>
        </div>

        <div class="item-group">
            <button class="btn btn-danger" type="submit">Delete</button>
            <a class="btn btn-primary" href="/storages">Back</a>
        </div>
    </form>
</div>*@